% ============================================================================
% CAEM defence poster — pre-thesis-2 milestone
% Confidence-Aware Episodic Memory with Self-Improvement for Progressive
% Hallucination Reduction in Large Language Models
% CSE400 · Department of CSE · BRAC University
% Authors: Md. Aksan Gony Alif, Maliha Binte Shamim, Md. Mahmudur Rahman
% Supervisor: Dr. Md. Golam Rabiul Alam · Co-supervisor: Md. Tanzim Reza
% Built on Anish Athalye's Gemini beamerposter theme (MIT-licensed).
% Custom colour theme: beamercolorthemecaem.sty (BRAC navy + thesis figure palette).
% Structure aligned with BRAC Pre-Thesis 2 sample layout.
% ============================================================================

\documentclass[final]{beamer}

% ====================
% Packages
% ====================

\usepackage[T1]{fontenc}
% luainputenc is obsolete under modern LuaTeX (UTF-8 is the native input
% encoding), so we skip it; inputenc is loaded automatically by LuaLaTeX.
\usepackage{lmodern}
\usepackage[size=custom, width=120,height=72, scale=1]{beamerposter}
\usetheme{gemini}
\usecolortheme{caem}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{array}
\usepackage{colortbl}      % provides \rowcolor for shaded table rows
\usepackage[table]{xcolor} % colour names + table-colour support
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,shapes.geometric,fit,backgrounds}
\usepackage{pgfplots}
\pgfplotsset{compat=1.14}
\usepackage{anyfontsize}

% ====================
% Lengths (three-column layout: separators + columns sum to paperwidth)
% ====================

\newlength{\sepwidth}
\newlength{\colwidth}
\setlength{\sepwidth}{0.025\paperwidth}
\setlength{\colwidth}{0.3\paperwidth}

\newcommand{\separatorcolumn}{\begin{column}{\sepwidth}\end{column}}

% ====================
% Title block
% ====================

\title{CAEM: Confidence-Aware Episodic Memory with Self-Improvement}

% Authors, institute, and footer content are rendered directly by the
% overridden headline / footline templates in beamercolorthemecaem.sty.
% The previous \author / \institute / \footercontent commands have been
% removed because the BRAC Pre-Thesis 2 sample layout hardcodes the names,
% the department line, and the four footer segments inside the templates.

% ====================
% Body
% ====================

\begin{document}

\begin{frame}[t]
\begin{columns}[t]
\separatorcolumn

% ============================================================================
% COLUMN 1: Abstract + Research Objective + Methodology
% ============================================================================

\begin{column}{\colwidth}

  \begin{block}{Abstract}
    Large language models produce confident but factually incorrect outputs
    at rates that span three to ninety-one percent across deployment
    domains, a failure mode that increased parameter count alone does not
    close. This thesis proposes Confidence-Aware Episodic Memory with
    Self-Improvement (CAEM), an architectural intervention that treats
    hallucination reduction as a system-level problem rather than a
    post-hoc detection problem. A single query passes through a verified
    episodic memory store, a confidence-aware three-tier router with an
    independent safety override, and a nine-signal verifier ensemble.
    Against the matched zero-shot baseline, the within-trajectory best
    cycle reaches a pooled exact-match gain of $+20.3\%$ and a pooled
    composite hallucination metric reduction of $-29.4\%$ on the training
    panel. Against the locked external eight-baseline panel, CAEM leads on
    both pooled axes at the best and final cycles.
  \end{block}

  \begin{block}{Research Objective}
    \begin{itemize}
      \item \textbf{Verified episodic memory with a four-outcome decision:}
        design an episodic memory whose admission rule is finer than a
        single binary threshold.
      \item \textbf{Pre-generation confidence, three-tier routing with
        safety override, and a retrieval-utility classifier:} compute a
        pre-generation confidence estimate from a single short forward
        pass on the query that fuses a short-greedy-decode token-probability
        aggregate with an encoder-layer convergence ratio, calibrate it
        via per-benchmark temperature scaling on a disjoint fold, and use
        it independently from the memory similarity score and the
        stored-answer quality.
      \item \textbf{Nine-signal post-generation verifier with a calibrated
        probability composite:} combine nine complementary signals into a
        per-answer record across four families: internal calibration of
        the generator, sample-set agreement across resampled chains,
        external grounding against retrieved evidence under three
        aggregation rules, and question-answer relevance.
      \item \textbf{Fixed-threshold storage gate with two registered
        cut-points:} convert the calibrated probability composite into the
        storage and deferral decisions through a fixed-threshold rule
        whose two cut-points and one abstention floor are registered
        before the main run begins and held constant across cycles.
      \item \textbf{Constrained self-improvement with a parameter-efficient
        adapter and a multi-modal retention guard:} curate the fine-tuning
        pool from stored episodes that exceed a strict quality threshold
        set above the storage cut-point and pass the curated pool through
        a benchmark-balancing reweighting layer that prevents
        single-benchmark dominance.
    \end{itemize}
  \end{block}

  \begin{block}{Methodology}
    The architecture organises a single query into an eight-stage pipeline
    traversed once per query and wrapped in a per-cycle update loop.
    Stage 1 produces a pre-routing confidence from a single short forward
    pass. Stage 2 encodes the query and probes a similarity-indexed
    episodic memory for the nearest stored entry. Stage 3 combines the
    similarity and stored-quality scores into a dispatch score and routes
    to one of three tiers, with a safety override on low confidence.
    Stage 3b, a retrieval-utility classifier, refines the dispatch into
    zero-shot or retrieval-augmented tier per query. Stage 4 executes the
    chosen tier; Stage 5 passes the answer through a nine-signal
    post-generation verifier; Stage 6 applies a four-outcome decision
    (store, defer, abstain, discard); Stage 7 returns the user-facing
    action. Stage 8, at cycle boundaries, runs retroactive re-verification,
    deferred reconsideration, memory consolidation, and a regularised
    fine-tune under a retention guard with asymmetric rollback: weights
    are rolled back on retention failure, but memory is preserved.
  \end{block}

\end{column}

\separatorcolumn

% ============================================================================
% COLUMN 2: Proposed Model Architecture + Locked Configuration + Trajectory
% ============================================================================

\begin{column}{\colwidth}

  \begin{block}{Proposed Model Architecture}
    \includegraphics[width=0.95\colwidth,keepaspectratio]{figures/architecture_source.pdf}

    The eight-stage pipeline routes each query through pre-routing
    confidence, memory lookup, three-tier dispatch with the Stage 3b
    retrieval-utility classifier, tier execution, nine-signal verification,
    the four-outcome decision, and output; Stage 8 closes every cycle
    boundary with retroverify, deferred reconsider, consolidate, LoRA
    fine-tune, and the multi-modal retention probe.
  \end{block}

  \begin{block}{Locked Configuration}
    \footnotesize
    \setlength{\tabcolsep}{8pt}
    \renewcommand{\arraystretch}{1.05}
    \begin{tabular}{@{}l l@{}}
      \toprule
      \textbf{Component} & \textbf{Value} \\
      \midrule
      Storage cut $\tau_{\text{store}}$ \,/\, $\tau_{\text{defer}}$ & $0.60$ \,/\, $0.45$ \\
      SIL admission $\tau_{\text{train}}$ & $0.70$ \\
      Retrieval-utility classifier $\tau_{\text{RUC}}$ & $0.375$ \\
      Retention floor $\rho_{\min}$ & $0.93$ \\
      Router $\lambda$ \,/\, safety $\phi_{\text{pg}}$ & $0.70$ \,/\, $0.38$ \\
      Backbone & Qwen-2.5-3B-Instruct (frozen) \\
      LoRA & $r{=}32$, $\alpha{=}64$, 7 projections \\
      Active verifier signals & $9$ \\
      Trajectory & registered $10$ \,/\, realised $6$ cycles \\
      \bottomrule
    \end{tabular}
  \end{block}

  \begin{block}{Per-Cycle Trajectory}
    \footnotesize
    \setlength{\tabcolsep}{5pt}
    \renewcommand{\arraystretch}{1.05}
    \resizebox{\colwidth}{!}{%
    \begin{tabular}{@{}l r r c c c l@{}}
      \toprule
      \textbf{Cycle} & \textbf{Pooled EM} & \textbf{Pooled CHM} & \textbf{MMLU} & \textbf{TQA-test} & \textbf{CSQA-test} & \textbf{Outcome} \\
      \midrule
      \rowcolor{lightgrey} B1 (zero-shot ref) & $0.507$ & $0.120$ & \,---\, & \,---\, & \,---\, & reference \\
      C0 & $0.541$ & $0.135$ & \,---\, & \,---\, & \,---\, & baseline \\
      \rowcolor{caemTealLight}
      \textbf{C2 $\star$} & $\mathbf{0.544}$ & $\mathbf{0.107}$ & $0.975$ & $0.949$ & $0.982$ & \textbf{COMMIT (best)} \\
      C3 & $0.532$ & $0.115$ & $0.992$ & $0.987$ & $0.976$ & COMMIT \\
      \rowcolor{caemRedLight}
      C4 & $0.532$ & $0.115$ & $1.018$ & $\mathbf{0.886}$ & $0.982$ & \textbf{ABORT} \\
      C5 & $0.517$ & $0.109$ & $0.999$ & $1.013$ & $0.958$ & COMMIT (post-rollback) \\
      \bottomrule
    \end{tabular}%
    }
  \end{block}

\end{column}

\separatorcolumn

% ============================================================================
% COLUMN 3: Headline Result + External Baselines + RUC Ablation + Future Work + References
% ============================================================================

\begin{column}{\colwidth}

  \begin{block}{Headline Result}
    Against the matched zero-shot baseline B1 on the same backbone, the
    training-panel architectural-contribution slice at the within-trajectory
    best cycle (C2) reaches a pooled exact-match gain of $+20.3\%$ relative
    and a pooled composite hallucination metric reduction of $-29.4\%$
    relative; the trajectory-end cycle holds at $+13.7\%$ EM and $-20.3\%$
    CHM. CAEM beats every external baseline on both axes at both reference
    cycles.

    \[
      \boxed{\Delta\mathrm{EM} = +20.3\%\ \text{rel.}} \qquad
      \boxed{\Delta\mathrm{CHM} = -29.4\%\ \text{rel.}}
    \]
    \centering\footnotesize
    (training panel, CAEM C2 vs.\ B1 zero-shot baseline)
  \end{block}

  \begin{block}{External Baseline Comparison}
    % Two side-by-side mini-tables save vertical space. CAEM reference cycles
    % + B1-B4 (closer baselines) on the left; B5-B8 (deeper deficits) on the
    % right. Same Delta-vs-C2 columns and bold-highlight pattern as the RUC
    % ablation table below for visual consistency.
    \begin{minipage}[t]{0.49\colwidth}
      \scriptsize
      \setlength{\tabcolsep}{3pt}
      \renewcommand{\arraystretch}{1.05}
      \begin{tabular}{@{}l r r r r@{}}
        \toprule
        \textbf{System} & \textbf{EM} & \textbf{CHM} & $\boldsymbol{\Delta}$\textbf{EM} & $\boldsymbol{\Delta}$\textbf{CHM} \\
        \midrule
        \rowcolor{caemTealLight}
        \textbf{CAEM C2 $\star$} & $\mathbf{0.544}$ & $\mathbf{0.107}$ & --- & --- \\
        CAEM C5 (final) & $0.517$ & $0.109$ & $-0.027$ & $+0.002$ \\
        \midrule
        B1 zero-shot & $0.507$ & $0.120$ & $+7.4\%$ & $-11.0\%$ \\
        B2 chain-of-thought & $0.500$ & $0.123$ & $+8.8\%$ & $-12.7\%$ \\
        B3 DPR-RAG & $0.458$ & $0.148$ & $+18.8\%$ & $-27.7\%$ \\
        B4 CoT$+$DPR-RAG & $0.461$ & $0.142$ & $+17.9\%$ & $-24.6\%$ \\
        \bottomrule
      \end{tabular}
    \end{minipage}\hfill
    \begin{minipage}[t]{0.49\colwidth}
      \scriptsize
      \setlength{\tabcolsep}{3pt}
      \renewcommand{\arraystretch}{1.05}
      \begin{tabular}{@{}l r r r r@{}}
        \toprule
        \textbf{System} & \textbf{EM} & \textbf{CHM} & $\boldsymbol{\Delta}$\textbf{EM} & $\boldsymbol{\Delta}$\textbf{CHM} \\
        \midrule
        B5 five-shot CoT & $0.405$ & $0.136$ & $+34.2\%$ & $-21.4\%$ \\
        B6 vanilla FT (C5) & $0.325$ & $0.144$ & $+67.2\%$ & $-25.5\%$ \\
        B7 FLARE & $0.292$ & $0.153$ & $+86.3\%$ & $-29.9\%$ \\
        B8 semantic entropy & $0.505$ & $0.120$ & $+7.7\%$ & $-10.5\%$ \\
        \midrule
        \rowcolor{caemOrangeLight}
        \multicolumn{5}{@{}l}{\textbf{Wins-against (joint $\Delta$EM \& $\Delta$CHM):}} \\
        \rowcolor{caemOrangeLight}
        \multicolumn{5}{@{}l}{\textbf{CAEM C2 beats $8/8$; CAEM C5 beats $8/8$.}} \\
        \bottomrule
      \end{tabular}
    \end{minipage}
  \end{block}

  \begin{block}{Retrieval-Utility Classifier Ablation}
    \footnotesize
    \setlength{\tabcolsep}{4pt}
    \renewcommand{\arraystretch}{1.05}
    \resizebox{\colwidth}{!}{%
    \begin{tabular}{@{}l c c c c c@{}}
      \toprule
      \textbf{Benchmark} & \textbf{EM off} & \textbf{EM on} & $\boldsymbol{\Delta}$\textbf{EM (pp)} & \textbf{T3 off} & \textbf{T3 on} \\
      \midrule
      FEVER          & $0.503$ & $0.503$ & $0.0$            & $92.3\%$ & $70.7\%$ \\
      TriviaQA       & $0.437$ & $0.463$ & $+2.7$           & $95.7\%$ & $68.0\%$ \\
      \rowcolor{caemOrangeLight}
      \textbf{CommonsenseQA}  & $0.603$ & $\mathbf{0.743}$ & $\mathbf{+14.0}$ & $57.7\%$ & $20.3\%$ \\
      StrategyQA     & $0.617$ & $0.640$ & $+2.3$           & $100.0\%$ & $76.7\%$ \\
      \rowcolor{caemOrangeLight}
      \textbf{TruthfulQA}     & $0.210$ & $\mathbf{0.310}$ & $\mathbf{+10.0}$ & $99.0\%$ & $40.0\%$ \\
      \midrule
      \textbf{Pooled} & $\mathbf{0.474}$ & $\mathbf{0.532}$ & $\mathbf{+5.8}$ & $\mathbf{88.9\%}$ & $\mathbf{55.1\%}$ \\
      \bottomrule
    \end{tabular}%
    }
  \end{block}

  \begin{block}{References}
    \scriptsize
    [1] Guo et al. \emph{ICML 2017} (calibration). \,
    [2] Lin et al. \emph{ACL 2022} (TruthfulQA). \,
    [3] Thorne et al. \emph{NAACL 2018} (FEVER). \,
    [4] Joshi et al. \emph{ACL 2017} (TriviaQA). \,
    [5] Talmor et al. \emph{NAACL 2019} (CommonsenseQA). \,
    [6] Hu et al. \emph{ICLR 2022} (LoRA). \,
    [7] Tang et al. \emph{ACL 2024} (MiniCheck). \,
    [8] Cherian et al. \emph{NeurIPS 2024} (boost layer).
  \end{block}

\end{column}

\separatorcolumn
\end{columns}
\end{frame}

\end{document}
