# scripts/ -- CAEM experiment-phase entry points
#
# run_experiment.py        -- Cycle 0->N full orchestrator (default N=10)
# run_calibration.py       -- temperature scaling + signal weight calibration
# run_purity_validation.py -- three theory validation protocols (Chapter 5)
#
# Note: ablation and baseline runners are rebuilt post-unified-verifier
# (Session 42 onward) and re-added here when re-implemented.
