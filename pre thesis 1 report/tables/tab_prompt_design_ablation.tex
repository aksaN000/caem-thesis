% Placeholder. Auto-populated by scripts/compare_prompt_design.py
% after the two Step 7.0 runs (legacy prompts vs uniform scaffolded
% CoT with forced decoder prefix) complete. Once data exists:
%   PYTHONPATH=. python scripts/compare_prompt_design.py \
%       --old_dir outputs/cycle_0_pre_cot_prompt/eval \
%       --new_dir outputs/cycle_0/eval \
%       --output_tex "pre thesis 1 report/tables/tab_prompt_design_ablation.tex"
%
% Current contents: a minimal compile-safe stub so the full thesis
% compiles before the data arrives. Uses htbp (not H) to avoid
% depending on the float package being loaded in the preamble; all
% underscores that would otherwise land in text mode are escaped.
\begin{table}[htbp]
\centering
\small
\caption[Prompt-design ablation at Cycle 0 (placeholder)]%
{Placeholder table. Auto-populated from
\texttt{outputs/cycle\_0\_pre\_cot\_prompt/eval} and
\texttt{outputs/cycle\_0/eval} by
\texttt{scripts/compare\_prompt\_design.py} once both Step~7.0 runs
complete. The real table reports per-benchmark old-vs-new EM, mean
$u_{\text{stored}}$, and mean prediction length in words under the
legacy per-task minimal-CoT prompts versus the uniform few-shot
scaffolded CoT prompt with forced decoder prefix.}
\label{tab:prompt-design-ablation}
\begin{tabular}{lccc}
\toprule
Benchmark & EM $\Delta$ & $u_{\text{stored}}$ $\Delta$ & Mean words $\Delta$ \\
\midrule
\multicolumn{4}{c}{\emph{To be populated.}} \\
\bottomrule
\end{tabular}
\end{table}
