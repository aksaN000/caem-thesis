% ============================================================================
% Appendix A — Implementation reference
% Status: SKELETON. Phase C populates this with the implementation
% references that body prose deliberately omits.
%
% Purpose: body prose in chapters one through nine is concept-only
% (no module paths, no class or function names, no script names, no
% folder or file references, no command-line flags, no backend
% strings). All of those references that a re-implementer needs live
% here.
% ============================================================================

\chapter{Implementation reference}
\label{app:implementation}

\section{Repository layout and module map}
\label{app:repo-layout}
% Top-level folders, the canonical package paths, and the role of each
% module.

\section{Configuration registry}
\label{app:config-registry}
% Every constant in the configuration object, its provenance tag (literature,
% design, calibrated), and its source.

\section{Hyperparameter listing}
\label{app:hyperparameters}
% A consolidated table of every hyperparameter, its value, its source, and the
% subsection of Chapter 4 that introduces it.

\section{Runbook step graph}
\label{app:runbook}
% The phase one runner steps in execution order, with input and output
% artefacts for each step.

\section{Snapshot reference}
\label{app:snapshot}
% The Hugging Face dataset path that holds the pre-step-7 snapshot. The
% manifest, the recovery command, and the version tag.

\section{Hardware and software profile}
\label{app:hardware}
% The rented hardware envelope, the framework versions, the determinism
% switches, and the seed convention.

\section{Calibration script reference}
\label{app:calibration-scripts}
% The three calibration entry points, their inputs and outputs, and the
% sample-count and dedup discipline that each enforces.

\section{Validation gate reference}
\label{app:gate-reference}
% The validation gate command, its four checks, and the threshold values it
% enforces.
