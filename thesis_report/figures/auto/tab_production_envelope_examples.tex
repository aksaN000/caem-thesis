% PLACEHOLDER -- auto-overwritten when the corresponding generator script runs.
% Keep this file in version control as a no-op stub so the thesis compiles
% before the empirical run lands. The replacing artefact will retain the same
% filename so every \input{} in the chapter source continues to resolve.
\begin{tcolorbox}[colback=gray!5,colframe=gray!40,arc=2pt,boxrule=0.5pt,left=8pt,right=8pt,top=6pt,bottom=6pt]
\textit{Placeholder.} The production-envelope sample table is pending the final main run; the generator script will overwrite this stub with the four-outcome rendering (store, defer, abstain, discard) at compile time.
\end{tcolorbox}
